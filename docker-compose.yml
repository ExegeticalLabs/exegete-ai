version: "3.9"
services:
  server:
    build: ./apps/server
    environment:
      - PORT=8787
    ports:
      - "8787:8787"

  web:
    build: ./apps/web
    environment:
      - VITE_SERVER_URL=http://server:8787
    ports:
      - "5173:5173"
    depends_on:
      - server
