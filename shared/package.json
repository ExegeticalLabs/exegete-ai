{
  "name": "cribbage-remote-deck-shared",
  "version": "1.0.0",
  "type": "module",
  "description": "Shared types and utilities for cribbage remote deck",
  "main": "types.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}
